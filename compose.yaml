services:
  nginx:
    image: nginx:alpine
    ports: 
      - 80:80
    volumes:
      - ./conf.d:/etc/nginx/conf.d
    networks:
      - nginx-net 
    container_name: nginx-main

networks:
  nginx-net:
    external: true
